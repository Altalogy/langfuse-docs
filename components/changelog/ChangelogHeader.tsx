// TODO: Reimplement using fumadocs source.getPages() and usePathname().
// Previously used getPagesUnderRoute("/changelog"), router.pathname, and useConfig() from Nextra which no longer exist.

export const ChangelogHeader = () => {
  // TODO: Get current changelog page metadata from fumadocs source and render header
  return null;
};
